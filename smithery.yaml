runtime: container

build:
  dockerfile: Dockerfile
  dockerBuildPath: .

startCommand:
  type: http
  command: uv
  args:
    - run
    - main.py
  configSchema:
    type: object
    properties: {}
    required: []